#include "monty.h"
/**
 * the_stack - print top
 * @head: the stack head
 * @counter: the line_number
 * Return:
*/
void the_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
